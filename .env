# DO NOT ADD SECRETS TO THIS FILE. This is a good place for defaults.
# If you want to add secrets use `.env.local` instead.

# The `.env.local` file is also the place to override any of these default
# values during local development.

# Server log level
# One of: "fatal" | "error" | "warn" | "info" | "debug" | "trace" | "silent"
PINO_LOG_LEVEL="info"

# Google Analytics Measurement ID
NEXT_PUBLIC_ANALYTICS_ID=""

# Default preload attribute for audio elements
# One of: "none" | "metadata" | "auto"
NEXT_PUBLIC_AUDIO_PRELOAD="none"
